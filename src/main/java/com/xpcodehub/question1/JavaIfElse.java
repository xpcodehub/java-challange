package com.xpcodehub.question1;

public class JavaIfElse {

// TODO Questão 1 do README.md
    public String solucao(int entrada) {
        return entrada % 2 == 1 || (entrada >= 6 && entrada <= 20 ) ? "Imprevisivel" : "Previsivel";
    }
}
